/*
 * Модуль для парсинга HEX-строк в массивы байт.
 * Необходим для передачи произвольных последовательностей (в т.ч. с нулями).
 * Зубехин Никита
 * МК-101
 */

#ifndef HEX_PARSER_H
#define HEX_PARSER_H

#include <stddef.h>

/*
 * Преобразует HEX-строку в массив байт.
 * Возвращает количество байт или -1 при ошибке.
 * Память для массива выделяется внутри.
 */
int hex_to_bytes(const char* hex_str, unsigned char** out_bytes);

#endif